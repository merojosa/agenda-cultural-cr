{
	"name": "backend",
	"version": "0.3.0",
	"private": true,
	"main": "dist/index.js",
	"scripts": {
		"start:local": "yarn build && sls invoke local --function update-theater-data",
		"build": "rm -rf dist && esbuild src/index.ts --bundle --platform=node --packages=external --target=node18 --outdir=dist",
		"deploy:dev": "tsc && yarn run build && sls deploy --stage dev",
		"lint": "prettier --check --ignore-path=../../.prettierignore . && eslint \"src\""
	},
	"dependencies": {
		"luxon": "^3.4.0",
		"puppeteer-core": "^21.0.3"
	},
	"devDependencies": {
		"@sparticuz/chromium": "^115.0.0",
		"@types/luxon": "^3.3.1",
		"@types/node": "^18.17.4",
		"esbuild": "^0.19.1",
		"serverless": "^3.34.0",
		"serverless-offline": "^12.0.4",
		"tsconfig": "*",
		"typescript": "^5.1.6"
	}
}
