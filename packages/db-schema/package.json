{
	"name": "db-schema",
	"version": "0.0.0",
	"private": true,
	"main": "./dist/schema.js",
	"types": "./src/schema.ts",
	"publishConfig": {
		"access": "public"
	},
	"devDependencies": {
		"drizzle-orm": "^0.28.2",
		"esbuild": "^0.19.2",
		"tsconfig": "*",
		"typescript": "^5.1.6"
	},
	"scripts": {
		"lint": "prettier --check --ignore-path=../../.prettierignore . && eslint \"src\"",
		"build": "rm -rf dist && esbuild src/schema.ts --bundle --platform=node --packages=external --target=node18 --outdir=dist"
	}
}
